@model IEnumerable<SetorPagamento.Models.Pagamento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pedido.PedidoId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorCliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pedido.PedidoId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorCliente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PagamentoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PagamentoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PagamentoId })
        </td>
    </tr>
}

</table>
